# DSH Mobile 接力模型强制规则与合格标准

本文件适用于在 `dsh-mobile-app-v1.0` 中继续工作的所有模型和开发者。开始修改前必须完整阅读 `docs/HANDOFF.md`、`docs/ARCHITECTURE.md`、`docs/PROTOCOL.md`、`docs/SECURITY.md` 与 `docs/DESIGN_SYSTEM.md`。

## 一、不可违反的产品与架构规则

1. **只在 v1.0 主线工作。** `dsh-mobile-app-v1.1` 是 DeepSeek v4-flash-0731 产生的缺陷样本，只能查阅，禁止整目录复制、合并或覆盖 v1.0。
2. **服务器是唯一权威。** DSH 服务、会话、上下文、工作区、Agent、模型选择和模型密钥均在服务器。Android 只做客户端和可失效缓存，不得创建会与服务器分叉的“手机独立会话数据库”。
3. **密钥永不下发手机。** 配对表单只能要求服务器地址、配对码等最小信息。DeepSeek/OpenAI/Anthropic 等 API Key 不得进入 WebView、localStorage、日志、截图、错误信息或网络响应。
4. **所有配置必须真的生效。** 任何设置按钮都必须绑定真实 DSH RPC、Android 能力或明确的本地偏好。协议没有能力时应显示“当前服务器版本不支持”和依据，禁止放一个看起来能用的假开关。
5. **不得伪造供应商、插件、市场或用量。** 模型目录来自 `session.models`；模型切换必须调用服务端并回读确认。插件只能展示服务器真实公开数据。余额只能来自官方余额接口。费用必须标“估算”。
6. **不另造 DSH。** 优先迁移 DSH 开源代码中的术语、图标、事件语义、交互顺序和状态反馈；若原组件与移动运行时强耦合，应写薄适配层或按同一规范重构，不能仅凭截图做静态仿制品。
7. **安全失败。** 网络、鉴权、协议能力和上传解析失败时必须给用户可理解的状态；不允许用静默 `catch`、假成功 toast、默认模型或占位数据掩盖错误。

## 二、UI 与交互规则

1. 颜色只使用设计 token，主体系为 DSH 的黑、白、灰、蓝；允许 Tokyo Night 作为第三主题，不引入霓虹渐变、彩色玻璃拟态或无来源的装饰色。
2. 移动端必须重新排布而非缩放桌面页面。触控目标不小于 44 CSS px；底部面板避开系统导航区；键盘弹出时输入框仍可见。
3. 品牌统一为黑色鲸鱼加 `DeepSeek Harness`，不要再出现 `DSH`、其他鲸鱼样式或重复 logo。
4. 输入框结构、发送/运行态、工作区、模式、权限、模型/推理等级、上下文环应尽可能与 DSH Web 一致。
5. 列表必须有 loading、empty、error、disabled、selected 和长文本截断状态；不能只设计理想状态。
6. 横竖屏都要可用。横屏不是把竖屏页面横向拉伸；抽屉、sheet、轨迹和输入框都必须重新核验可视高度。
7. 图标须来自项目统一图标层或兼容 DSH 的开源图标集，禁止混用 emoji、文字字符和多套笔画风格。

## 三、实现与调试纪律

1. 修改前先复现并记录可观察证据：RPC method、request id、HTTP status、服务端 envelope、event seq、projection asOfSeq 或 Android logcat。不要凭感觉改 CSS/状态变量。
2. 修 bug 要定位根因。例：模型切换的合格修复是“服务端 select + 服务端 models 回读一致”，不是只改按钮勾选；主题修复要覆盖 DOM token、系统栏和持久化，不是只改一个 class。
3. 跨层变更必须画清链路：UI -> MobileApiClient -> carrier allowlist -> Host ApiProxy -> DSH service -> event/projection 回流。缺一层就不能宣称完成。
4. 协议响应需保持 request id 对应，订阅事件需处理重连、缺口补拉、重复 seq 和乱序保护。
5. 日志必须脱敏。严禁输出 Authorization、配对 token、API Key、完整用户文件内容或敏感服务器路径。
6. 新价格、模型别名、上传限制、协议能力等易变数据应集中在单一模块并带测试/来源日期，不散落硬编码。
7. 不依赖未说明的全局环境。开发 fixture、真实服务器和 Android WebView 三种环境的分支要显式、可诊断。
8. 保留用户已有修改；禁止破坏性 reset/覆盖。大重构前先确认现有测试基线。

## 四、必须通过的测试门槛

### A. 自动化

每次候选交付必须从仓库根目录重新执行并保存结果：

```text
pnpm check
pnpm test
pnpm build
pnpm package:gateway
pnpm build:android
```

任何命令失败都不能以“与本次无关”略过；要么修复，要么在交接报告写明失败命令、完整根因和阻塞条件。

新增逻辑至少覆盖：

- 模型切换后回读不一致必须报错；
- token 桶与峰/谷价格计算、未知模型和无基线状态；
- 余额接口成功、无凭据、上游 401/429/5xx、超时和响应结构异常；
- 历史事件 fold、重复/乱序 seq、工具调用与流式文本；
- 附件类型/大小边界和二进制拒绝；
- 缓存损坏/过期不能覆盖服务器新数据。

### B. 浏览器契约与视觉检查

使用 fixture 和真实网关分别检查：

- 360×800、393×852、412×915 竖屏；
- 至少一个约 844×390 的横屏；
- 配对、抽屉、设置全部 section、模型切换、预设锁定、归档、主题；
- 长会话、长标题、代码块、Markdown、Deep diving、工具展开、完整轨迹搜索/筛选；
- 文件、相册、拍照入口和预览删除；
- 输入法弹出、返回键、sheet 关闭顺序、横竖屏往返；
- console error 为 0，失败网络请求都能解释且 UI 不报假成功。

截图只能辅助验收，不能替代点击、网络、DOM 状态和 RPC 回读验证。

### C. 真实 DSH 服务器

至少证明：

1. App 选择模型 A 后，服务器 `session.models.current` 为 A；再切换 B 同样成立，实际下一轮响应使用 B；
2. 杀掉 App/清理 WebView 进程后重开，先显示缓存并最终与同一服务器会话一致；
3. 网络断开再恢复，事件没有重复消息或丢失 seq；
4. 工具调用、轨迹、tokenUsage projection 与服务器 Web 页面一致；
5. API 余额不暴露密钥，余额不可用时不显示伪金额；
6. 图片由 DSH DeepSeek adapter 上传；不支持的普通二进制文件明确失败。

### D. Android 真机/模拟器

- 全新安装与覆盖升级各测一次；
- `apksigner verify` 通过，包名和 `versionName/versionCode` 正确；
- Room/Keystore、返回键、系统栏、安全区、相机权限、FileProvider、文件选择器正常；
- 横竖屏切换后不会丢会话、重复发送、关闭流或重建出空白状态；
- 至少完成一次真实长流和运行中停止/返回行为测试；
- 记录 APK SHA-256。

## 五、完成定义（Definition of Done）

一项功能只有同时满足以下条件才可标记“完成”：

1. 用户可见交互已实现，所有状态齐全；
2. 后端/原生能力真实连通，不是 mock、静态数组或只改 UI；
3. 失败路径和安全边界已实现；
4. 对应自动化测试已添加并通过；
5. fixture、真实服务器或 Android 中至少有与风险相称的人工验证证据；
6. 文档、协议、版本和产物与源码一致；
7. `docs/HANDOFF.md` 中的状态已经更新。

以下情况一律不合格：仅截图像、仅按钮可点、仅 fixture 可用、吞掉异常、凭经验声称“应该可以”、旧 APK 冒充当前源码、把协议不支持的内容做成假配置。

## 六、交付与再次交接规则

1. 所有过程和结果文件都放在 v1.0 内，不散落到临时目录或 v1.1。
2. APK、网关 tgz、截图、测试报告分别进入 `artifacts/apk`、`artifacts/gateway`、`artifacts/screenshots`、`artifacts/reports`。
3. 每个产物要能对应源码版本；报告记录生成时间、命令、版本、哈希和是否签名。
4. 最终回复必须区分“已实现且验证”“已实现未实机验证”“未完成/受协议限制”，不得混写成一句“全部完成”。
5. 额度或时间可能中断时，优先更新 `docs/HANDOFF.md`，写清最后一个成功测试、正在编辑的文件、下一条安全命令和未决风险。

